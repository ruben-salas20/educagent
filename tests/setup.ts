// tests/setup.ts
// Setup file referenciado por vitest.config.ts.
// Vacío por ahora — punto de inyección para globals/fixtures compartidos
// en tests futuros (FakeClock, stubs LLM, etc.).
export {};
